<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>
        <a href="/note/new">Create a new note</a>
        <ul>
            <% for(let note of notes) { %>
                <li><a href="/note/<%= note.id %>"><%= note.title || 'Untitled' %></a></li>
            <% } %>
        </ul>
        <a href="/">Retour</a>
    </div>
</body>
</html>